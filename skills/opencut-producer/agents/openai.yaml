interface:
  display_name: "OpenCut Producer"
  short_description: "Build reviewed video candidates."
  default_prompt: "Use $opencut-producer to create three polished review candidates from these local video and audio assets, tune production controls, then open the OpenCut approval workspace for preview, single-final, or batch render approval."
